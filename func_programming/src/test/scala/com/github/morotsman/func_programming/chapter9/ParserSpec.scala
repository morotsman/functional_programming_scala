package com.github.morotsman.func_programming.chapter9

import org.scalatest._
import com.github.morotsman.func_programming.chapter8._

class ParserSpec extends FlatSpec with Matchers {

  "Parser" should "follow this properties" in {
    

    
    /*
    val smallInt = Gen.choose(-10, 10)
    val takeWhile = Prop.forAll(SGen.listOf(smallInt)) { ns =>

      val prop1 = ns.takeWhile(_ => false) == List()
      val prop2 = ns.takeWhile(_ => true) == ns
      val prop3 = ns.takeWhile(_ < 0).forall(_ < 0) == true

      prop1 && prop2

    }
    Prop.run(takeWhile)
    
    */
  }

}